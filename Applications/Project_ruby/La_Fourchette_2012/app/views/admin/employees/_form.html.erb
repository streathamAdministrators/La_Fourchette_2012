<%= form_for([:admin, @employee]) do |f| %>

  <% if @employee.errors.any? %>
    <div id="error_explanation" class="alert alert-error">
	  <button type="button" class="close" data-dismiss="alert">×</button>
      <h4 class="alert-heading"><%= pluralize(@employee.errors.count, "error") %> prohibited this admin_employee from being saved:</h4>

      <ul>
      <% @employee.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
	<p class="field">
		<%= f.label :is_active %>
    	<%= f.radio_button :is_active, true %> <%= f.label :is_active, "Oui" %>
		<%= f.radio_button :is_active, false %> <%= f.label :is_active, "Non" %>
    </p>
  
  	<p class="field">
   		<%= f.label :username %>
   		<%= f.text_field :username %>
 	</p>
 	
 	<p class="field">
   		<%= f.label :password %>
   		<%= f.password_field :password %>
 	</p>

	<p>
		<label>Password Confirmation: </label>
		<%= f.password_field :password_confirmation %>
	</p>
 	
 	<p class="field">
 		<%= f.label :restaurant_id %>
 		<%= collection_select(:employee, :restaurant_id, restaurant, :id, :name, { :prompt => 'Select a restaurant'}) %>
 	</p>


  <p class="actions">
    <%= f.submit :class => 'btn btn-primary btn-large'  %>
  </p>
<% end %>
