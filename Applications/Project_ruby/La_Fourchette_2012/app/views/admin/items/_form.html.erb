<%= form_for([:admin, @admin_item]) do |f| %>

  <% if @admin_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_item.errors.count, "error") %> prohibited this admin_item from being saved:</h2>
      <ul>
      <% @admin_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <p class="field">
   		<%= f.label :name %>
   		<%= f.text_field :name %>
 	</p>
 	
 	<p class="field">
   		<%= f.label :is_active %>
   		<%= f.text_field :is_active %>
 	</p>
 	
 	<p class="field">
   		<%= f.label :description %>
   		<%= f.text_field :description %>
 	</p>
 	
 	<p class="field">
   		<%= f.label :price %>
   		<%= f.text_field :price %>
 	</p>
 	
 	<p class="field">
   		<%= f.label :time %>
   		<%= f.text_field :time %>
 	</p>
 	
 	<p class="field">
   		<%= f.label :product_type %>
   		<%= collection_select(:admin_item, :product_type_id, product_type, :id, :name, { :prompt => 'select product type'}) %>
 	</p>
 	

  <div class="actions">
    <%= f.submit :class => 'btn btn-primary btn-large' %>
  </div>
<% end %>
