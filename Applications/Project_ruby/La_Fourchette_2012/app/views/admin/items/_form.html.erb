<%= form_for([:admin, @item]) do |f| %>

  <% if @item.errors.any? %>
    <div id="error_explanation" class="alert alert-error">
	  <button type="button" class="close" data-dismiss="alert">×</button>
      <h4 class="alert-heading"><%= pluralize(@item.errors.count, "error") %> prohibited this admin_item from being saved:</h2>
      <ul>
      <% @item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <p class="field">
   		<%= f.label :name %>
   		<%= f.text_field :name %>
 	</p>
 	
 	<p class="field">
		<%= f.label :is_active %>
    	<%= f.radio_button :is_active, true %> <%= f.label :is_active, "Yes" %>
		<%= f.radio_button :is_active, false %> <%= f.label :is_active, "No" %>
    </p>
 	
 	<p class="field">
   		<%= f.label :description %>
   		<%= f.text_field :description %>
 	</p>
 	
 	<p class="field">
   		<%= f.label :price %>
   		<%= f.text_field :price %>
 	</p>
 	
 	<p class="field">
   		<%= f.label :time %>
   		<%= f.text_field :time %>
 	</p>
 	
 	<p class="field">
   		<%= f.label :product_type %>
   		<%= collection_select(:item, :product_type_id, product_type, :id, :name, { :prompt => 'select product type'}) %>
 	</p>
 	

  <div class="actions">
    <%= f.submit :class => 'btn btn-primary btn-large' %>
  </div>
<% end %>
